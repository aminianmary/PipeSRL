package Sentence;

/**
 * Created by monadiab on 8/3/16.
 */
public enum ArgumentPosition {
    //with respect to predicate (some nominal predicates are their own arguments in conll data)
    BEFORE,
    ON,
    AFTER
}
